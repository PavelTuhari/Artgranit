# ============================================================================
# Oracle OCI Database Configuration
# ============================================================================
# Скопируйте этот файл в .env и заполните реальными значениями
# cp .env.example .env
# ============================================================================

# Oracle Database User
DB_USER=ADMIN

# Oracle Database Password (НЕ КОММИТЬ В GIT!)
DB_PASSWORD=ArtG2025UNAmd##

# Oracle Wallet Password (НЕ КОММИТЬ В GIT!)
WALLET_PASSWORD=UniSim2025UNAmd__

# Oracle Wallet директория — путь к распакованной папке wallet.
# Рекомендуется вне проекта (на сервере напр. /home/ubuntu/wallet).
# Обновления кода не меняют логику подключения.
WALLET_DIR=wallet_HXPAVUNKCLU9HE7Q

# TNS Alias (опционально)
TNS_ALIAS=hxpavunkclu9he7q_high

# Oracle Connect String (TNS connect string)
CONNECT_STRING=(description= (retry_count=20)(retry_delay=3)(address=(protocol=tcps)(port=1522)(host=adb.eu-frankfurt-1.oraclecloud.com))(connect_data=(service_name=g47056ff8b1b3d4_hxpavunkclu9he7q_high.adb.oraclecloud.com))(security=(ssl_server_dn_match=yes)))

# ============================================================================
# Application Configuration
# ============================================================================

# Secret Key для Flask (сгенерируйте уникальный ключ для production)
SECRET_KEY=dev-secret-key-change-in-production

# Environment: LOCAL или REMOTE
ENVIRONMENT=LOCAL

# Server Host (0.0.0.0 для доступа из сети)
SERVER_HOST=0.0.0.0

# Server Port
PORT=3003

# ============================================================================
# Authentication (опционально, если отличается от DB_USER/DB_PASSWORD)
# ============================================================================

# Default Username для веб-интерфейса
DEFAULT_USERNAME=ADMIN

# Default Password для веб-интерфейса
DEFAULT_PASSWORD=ArtG2025UNAmd##

# ============================================================================
# Remote Server Configuration (опционально)
# ============================================================================

REMOTE_SERVER_HOST=92.5.3.187
REMOTE_SERVER_PORT=8000

# ============================================================================
# EasyCredit API (creditare produselor Bomba, sandbox/production)
# ============================================================================
# Ключ интеграции: user + passwd согласно API (Preapproved, Request, Status, ECM_*).
# Тест: http://w91.ecredit.md:49082, прод: https://w81.ecredit.md:8082

# EASYCREDIT_ENV=sandbox
# EASYCREDIT_BASE_URL=
# EASYCREDIT_API_USER=
# EASYCREDIT_API_PASSWORD=

# ============================================================================
# Weather API (опционально)
# ============================================================================

# OpenWeatherMap API Key (если используется)
# OPENWEATHER_API_KEY=your_api_key_here

# Weather City (если не указан, определяется автоматически по IP)
# WEATHER_CITY=Moscow
