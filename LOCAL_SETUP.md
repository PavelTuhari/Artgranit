# 🖥️ Локальный запуск на Mac

## Быстрый старт

### Запуск на localhost:3003

```bash
./run_local.sh
```

Или вручную:

```bash
source venv/bin/activate
export ENVIRONMENT=LOCAL
export PORT=3003
python3 app.py
```

## 📍 Доступные URL

После запуска приложение доступно по адресам:

- **Главная страница:** http://localhost:3003
- **SQL Developer:** http://localhost:3003/UNA.md/orasldev
- **Dashboard:** http://localhost:3003/UNA.md/orasldev/dashboard
- **Тестовая страница:** http://localhost:3003/test.html
- **API статус:** http://localhost:3003/api/status

## 🔧 Конфигурация

Параметры окружения устанавливаются автоматически через скрипт `run_local.sh`:

- `ENVIRONMENT=LOCAL` - локальный режим
- `PORT=3003` - порт для локального запуска
- `SERVER_HOST=127.0.0.1` - локальный хост

## 🆚 Сравнение режимов

| Параметр | Локальный (Mac) | Удаленный сервер |
|----------|----------------|------------------|
| Хост | 127.0.0.1 | 0.0.0.0 |
| Порт | 3003 | 8000 |
| URL | localhost:3003 | 92.5.3.187:8000 |
| ENVIRONMENT | LOCAL | REMOTE |

## 🛑 Остановка сервера

Для остановки сервера нажмите `Ctrl+C` в терминале, где запущен скрипт.

Или убейте процесс:

```bash
lsof -ti:3003 | xargs kill -9
```

## 🔍 Проверка статуса

```bash
# Проверить, работает ли сервер
curl http://localhost:3003/api/status

# Проверить, какой процесс использует порт
lsof -ti:3003
```

## ⚠️ Troubleshooting

### Порт 3003 занят

```bash
# Освободить порт
lsof -ti:3003 | xargs kill -9
```

### Зависимости не установлены

```bash
source venv/bin/activate
pip install -r requirements.txt
```

### Oracle Wallet не найден

Убедитесь, что файл `Wallet_HXPAVUNKCLU9HE7Q.zip` находится в корне проекта.
Wallet будет автоматически извлечен при первом подключении.

