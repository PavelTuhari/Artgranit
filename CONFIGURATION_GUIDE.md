# 🔧 Руководство по настройке подключения к Oracle OCI Database

## Обзор

**⚠️ ВАЖНО:** Все параметры подключения к Oracle OCI Autonomous Database **обязательно** должны храниться в файле `.env`. В коде **НЕТ** захардкоженных паролей - все значения читаются только из защищенного `.env` файла. Это соответствует современным практикам безопасности Python проектов.

**🔒 Безопасность:** Если `.env` файл не существует или не содержит нужных параметров, приложение не сможет подключиться к базе данных. Это сделано специально для безопасности - пароли никогда не должны храниться в коде!

## 📋 Быстрый старт

### Вариант 1: Использование графического интерфейса (рекомендуется)

1. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

2. **Запустите конфигуратор:**
   ```bash
   python3 configure_oracle_db.py
   ```

3. **В открывшемся окне:**
   - Заполните все поля подключения
   - Нажмите "🧪 Тест подключения" для проверки
   - Нажмите "💾 Сохранить настройки" для сохранения в `.env`

### Вариант 2: Ручная настройка

1. **Скопируйте шаблон:**
   ```bash
   cp .env.example .env
   ```

2. **Отредактируйте `.env` файл:**
   ```bash
   nano .env  # или используйте любой текстовый редактор
   ```

3. **Заполните параметры:**
   - `DB_USER` - имя пользователя Oracle
   - `DB_PASSWORD` - пароль пользователя Oracle
   - `WALLET_PASSWORD` - пароль для Oracle Wallet
   - `WALLET_ZIP` - имя файла Wallet ZIP (или полный путь)
   - `WALLET_DIR` - папка для распаковки Wallet
   - `CONNECT_STRING` - строка подключения TNS

## 📁 Структура файлов

```
.
├── .env                    # Файл с реальными настройками (НЕ КОММИТИТЬ!)
├── .env.example            # Шаблон конфигурации (можно коммитить)
├── config.py               # Читает настройки из .env
├── configure_oracle_db.py  # GUI конфигуратор
└── .gitignore              # Исключает .env из git
```

## 🔒 Безопасность

### ✅ Что сделано для защиты:

1. **`.env` файл исключен из git** (добавлен в `.gitignore`)
2. **Пароли хранятся только в `.env`**, не в коде
3. **`.env.example`** содержит только шаблоны без реальных паролей
4. **`config.py`** читает из переменных окружения

### ⚠️ Важные правила:

- ❌ **НИКОГДА** не коммитьте `.env` файл в git
- ✅ Используйте `.env.example` как шаблон
- ✅ Храните `.env` локально и на сервере отдельно
- ✅ Используйте разные пароли для разных окружений (dev/prod)

## 🔍 Параметры подключения

### Обязательные параметры:

| Параметр | Описание | Пример |
|----------|----------|--------|
| `DB_USER` | Имя пользователя Oracle | `ADMIN` |
| `DB_PASSWORD` | Пароль пользователя | `YourPassword123##` |
| `WALLET_PASSWORD` | Пароль для Oracle Wallet | `WalletPass123__` |
| `WALLET_ZIP` | Имя файла Wallet ZIP | `Wallet_HXPAVUNKCLU9HE7Q.zip` |
| `WALLET_DIR` | Папка для Wallet | `wallet_HXPAVUNKCLU9HE7Q` |
| `CONNECT_STRING` | TNS строка подключения | `(description=...)` |

### Опциональные параметры:

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `TNS_ALIAS` | TNS Alias (если используется) | `hxpavunkclu9he7q_high` |
| `SECRET_KEY` | Flask secret key | `dev-secret-key-change-in-production` |
| `ENVIRONMENT` | Окружение: LOCAL или REMOTE | `LOCAL` |
| `SERVER_HOST` | Хост сервера | `0.0.0.0` |
| `PORT` | Порт сервера | `3003` |

## 🧪 Тестирование подключения

### Через GUI:

1. Запустите `configure_oracle_db.py`
2. Заполните параметры
3. Нажмите "🧪 Тест подключения"
4. Если подключение успешно, увидите сообщение с временем сервера

### Через Python:

```python
from config import Config
from models.database import DatabaseConnection

try:
    conn = DatabaseConnection.get_connection()
    with conn.cursor() as cursor:
        cursor.execute("SELECT SYSDATE FROM DUAL")
        result = cursor.fetchone()
        print(f"✅ Подключение успешно! Время сервера: {result[0]}")
    conn.close()
except Exception as e:
    print(f"❌ Ошибка подключения: {e}")
```

## 🔄 Миграция со старой конфигурации

Если у вас уже есть `config.py` с жестко заданными значениями:

1. **Запустите конфигуратор:**
   ```bash
   python3 configure_oracle_db.py
   ```

2. **Или создайте `.env` вручную:**
   ```bash
   cp .env.example .env
   # Скопируйте значения из config.py в .env
   ```

3. **Удалите жестко заданные пароли из `config.py`** (они уже обновлены автоматически)

## 🐛 Устранение неполадок

### Ошибка: "ModuleNotFoundError: No module named 'dotenv'"

**Решение:**
```bash
pip install python-dotenv
# или
pip install -r requirements.txt
```

### Ошибка: ".env file not found"

**Решение:**
- Создайте `.env` файл из шаблона: `cp .env.example .env`
- Или используйте конфигуратор: `python3 configure_oracle_db.py`

### Ошибка: "Wallet ZIP file not found"

**Решение:**
- Убедитесь, что файл Wallet ZIP существует
- Проверьте путь в `WALLET_ZIP` (может быть имя файла или полный путь)
- Если файл в другой директории, укажите полный путь

### Ошибка подключения к базе данных

**Проверьте:**
1. Правильность `DB_USER` и `DB_PASSWORD`
2. Правильность `WALLET_PASSWORD`
3. Правильность `CONNECT_STRING`
4. Наличие интернет-соединения
5. Доступность Oracle Cloud сервера

## 📚 Дополнительная информация

- **Документация python-dotenv:** https://pypi.org/project/python-dotenv/
- **Oracle Database Connection:** https://python-oracledb.readthedocs.io/
- **Oracle Wallet:** https://docs.oracle.com/en/database/oracle/oracle-database/

## ✅ Чеклист настройки

- [ ] Установлен `python-dotenv`: `pip install python-dotenv`
- [ ] Создан `.env` файл: `cp .env.example .env`
- [ ] Заполнены все обязательные параметры в `.env`
- [ ] Протестировано подключение через `configure_oracle_db.py`
- [ ] `.env` добавлен в `.gitignore` (уже сделано)
- [ ] Приложение успешно запускается и подключается к БД

---

**Автор:** Разработано в рамках проекта Artgranit OCI  
**Версия:** 1.0  
**Дата:** 2025-12-03

