# t° CPU для мониторинга перегрева (TBControl). Читает coretemp через cpu_temp.sh.
# cpu.temp[1] = CPU1, cpu.temp[2] = CPU2. Совпадает по смыслу с ipmitool CPU1/CPU2 Temp.
UserParameter=cpu.temp[*],/usr/local/bin/cpu_temp.sh $1
