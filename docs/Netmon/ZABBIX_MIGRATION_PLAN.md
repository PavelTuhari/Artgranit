# План миграции Zabbix 3.4.15 → Zabbix 8.0 LTS

Документ подготовлен 12.09.2026 по результатам обследования действующей
системы мониторинга. Все цифры получены с боевых систем, не оценочные.

Связанные документы: [MONITORING_SYSTEM.md](MONITORING_SYSTEM.md) — как устроена
система сейчас; [TEST_REPORT.html](TEST_REPORT.html) — акт испытаний, где
устаревшая версия зафиксирована как замечание SEC-07.

---

## 1. Резюме и рекомендация

**Обновление «на месте» невозможно.** Действующий Zabbix 3.4.15 работает на
CentOS 7.9 с MariaDB 5.5 и PHP 5.6 — все три компонента сняты с поддержки
(CentOS 7 — 30.06.2024, MariaDB 5.5 — 2020, PHP 5.6 — 2018). Zabbix 8.0
требует PHP 8.0+ и MariaDB 10.5+ / MySQL 8.0.30+; поднять их на CentOS 7
штатными средствами нельзя.

**Рекомендуемый вариант — новая виртуальная машина KVM на том же
гипервизоре PROXMOX3** с Ubuntu 22.04 LTS и Zabbix 8.0 LTS, с параллельной
работой старого и нового сервера в течение 2 недель.

Обоснование выбора именно KVM, а не контейнера LXC, — в разделе 4: на
PROXMOX3 стоит Proxmox 4.4 с ядром 4.4, и контейнер с современной ОС на нём
попросту не запустится, тогда как у виртуальной машины собственное ядро.
Совместимость подтверждена практикой: на этом же хосте уже работает
ВМ 120 `ubuntu22`.

| Параметр решения | Значение |
|---|---|
| Куда | KVM-ВМ на PROXMOX3 (`192.168.0.149`), хранилище `local-zfs` |
| ОС | Ubuntu 22.04.3 LTS (ISO уже загружен на гипервизор) |
| Zabbix | 8.0 LTS, пакеты из `repo.zabbix.com` (Ubuntu 22.04 поддерживается) |
| СУБД | MariaDB 10.6 из репозитория Ubuntu |
| Ресурсы ВМ | 4 vCPU, 8 ГБ RAM, 100 ГБ диск |
| Данные | конфигурация переносится полностью, история — по варианту из раздела 8 |
| Простой мониторинга | **0 минут** (старый сервер работает до переключения) |
| Трудоёмкость | 2 рабочих дня работ + 2 недели параллельной работы |

---

## 2. Что есть сейчас: полная инвентаризация

### 2.1 Платформа

| Параметр | Значение | Статус |
|---|---|---|
| Гипервизор | Proxmox VE **4.4-1**, ядро 4.4.35-1-pve (2016 г.) | снят с поддержки |
| Размещение Zabbix | LXC-контейнер **CT 101** `zabbix34` | — |
| ОС контейнера | CentOS Linux **7.9.2009** | EOL 30.06.2024 |
| Ресурсы контейнера | 2 ядра, 3096 МБ RAM, диск 5.4 ТБ (занято 8.1 ГБ) | мало CPU/RAM |
| IP | `192.168.0.110`, шлюз `192.168.0.101` | — |
| Веб-сервер | Apache 2.4.6 | устарел |
| PHP | **5.6.40** | EOL 2018, Zabbix 8.0 требует 8.0+ |
| СУБД | MariaDB **5.5.68** | EOL 2020, Zabbix 8.0 требует 10.5+ |
| Zabbix | **3.4.15** | EOL 2018 |

### 2.2 Объём данных

| Показатель | Значение |
|---|---|
| База `zabbix` целиком | **1 532,7 МБ** |
| `history_uint` | 632,8 МБ (6 597 378 строк) |
| `history` | 497,7 МБ (5 061 478 строк) |
| `trends_uint` | 255,4 МБ (3 138 941 строка) |
| `trends` | 117,2 МБ (1 358 985 строк) |
| **Конфигурация без истории** | **21,3 МБ** ← именно это обязательно переносить |
| Глубина хранения | история 90 дней, тренды 365 дней, события 365 дней |
| Конфиги `/etc/zabbix` | 85 КБ |
| Скрипты `/usr/lib/zabbix` | 1,5 МБ |

**Вывод, определяющий стратегию:** ценная часть — 21 МБ конфигурации.
1,5 ГБ истории — вторичный актив со сроком жизни 90 дней.

### 2.3 Объекты мониторинга

| Объект | Количество |
|---|---|
| Хосты | **77** = 64 включённых + **13 выключенных** |
| Шаблоны | 53 (из них ~7 собственных) |
| Элементы данных | **2 734** (включено 2 561) |
| Триггеры | 1 699 |
| Графики | 427 |
| Действия | 10 (включено 1 — «Telegram») |
| Пользователи | 6 |
| Группы хостов | 18 |
| Веб-сценарии | 11 |
| Экраны / карты | 14 / 1 |
| Правила сетевого обнаружения | 1 |

### 2.4 Типы элементов — что потребует отдельной настройки

| Тип | Кол-во | Что нужно на новом сервере |
|---|---|---|
| Zabbix agent (пассивный) | 1 781 | ничего, работает из коробки |
| Zabbix agent (активный) | 282 | **переконфигурировать агенты**: `ServerActive` на новый IP |
| Простые проверки (ICMP) | 222 | пакет `fping` |
| JMX | 87 | **Zabbix Java gateway** (сейчас не установлен — не работают) |
| Внутренние | 85 | из коробки |
| Веб-сценарии | 66 | из коробки |
| SNMP v1/v2 | 81 | пакеты `snmp`, MIB-файлы |
| Внешние скрипты | 57 | **перенести 8 скриптов**, см. 2.5 |
| Мониторинг БД (ODBC) | 39 | **unixODBC + драйвер Oracle** |
| IPMI | 19 | `OpenIPMI`, включить `StartIPMIPollers` |
| Вычисляемые | 15 | из коробки |

### 2.5 Внешние скрипты — главный источник трудозатрат

| Скрипт | Язык | Зависимость | Действие при миграции |
|---|---|---|---|
| `pyora.py` | **Python 2.7** + `cx_Oracle` | Oracle client | **переписать** на Python 3 + `oracledb` (тонкий режим, клиент не нужен) |
| `cloudbd_count_sessions.sh` | bash | `sqlplus` | поставить Oracle Instant Client либо переписать на `oracledb` |
| `avpmr_count_messages.sh` | bash | `sqlplus` | то же |
| `stanby_status.sh` | bash | `sqlplus` | то же |
| `gcc_stanby_status.sh` | bash | `sqlplus` | то же |
| `ssl_check.sh` | sh | `openssl` | перенести как есть |
| `smartctl.sh` | bash | `smartmontools` | перенести как есть |
| `telegram_bot.sh` (alertscript) | bash | `curl` | **перенести вместе с токеном бота** |

На Ubuntu 22.04 интерпретатора `python2` нет. `pyora.py` придётся портировать —
это единственная работа в проекте, требующая написания кода.

### 2.6 Что уже не работает и переносу не подлежит

При обследовании выявлено **72 неработающих элемента** из 2 561 включённых:

| Тип | Всего | Сломано | Причина |
|---|---|---|---|
| Мониторинг БД (ODBC) | 38 | **38 (все)** | Oracle-мониторинг хоста `192.168.0.18` не работает целиком |
| Zabbix agent | 1 612 | 21 | агент недоступен или ключ не поддерживается |
| Внутренние | 85 | 6 | — |
| Активные агенты | 280 | 5 | — |
| Внешние скрипты | 56 | 1 | — |
| Вычисляемые | 15 | 1 | — |
| JMX | 87 | не опрашиваются | Java gateway не установлен |

**Миграция — повод это вычистить.** Переносить 38 заведомо мёртвых
ODBC-элементов и 87 неопрашиваемых JMX бессмысленно: либо чинить осознанно,
либо не переносить.

Сюда же **13 выключенных хостов** из 77: они не опрашиваются и, судя по
статусу, оставлены «на всякий случай». По каждому нужно решение — переносим
или списываем. Реально работающих хостов, требующих переноса, — **64**.

Выгрузка конфигурации уже снята (12.09.2026) инструментом из раздела 11:
53 шаблона и 75 хостов, 752 объекта, XML формата 3.4, 1,2 МБ. Файлы
проверены на целостность разбором XML.

---

## 3. Почему нельзя обновиться на месте

Три независимые блокировки, каждой достаточно:

1. **PHP.** Zabbix 8.0 frontend требует PHP ≥ 8.0. На CentOS 7 штатный
   PHP — 5.6; сборки 8.x есть только в сторонних репозиториях (Remi) и
   тянут за собой конфликт зависимостей с системным Apache.
2. **СУБД.** Zabbix 8.0 требует MariaDB ≥ 10.5 (или MySQL ≥ 8.0.30).
   Установленная MariaDB 5.5 отличается на 5 мажорных версий; апгрейд
   5.5 → 10.6 внутри CentOS 7 — отдельный рискованный проект.
3. **ОС.** CentOS 7 не получает обновлений с 30.06.2024. Строить новый
   сервер мониторинга на снятой с поддержки ОС — значит сразу закладывать
   следующую миграцию.

Дополнительно: контейнер LXC с CentOS 7 **нельзя** обновить «внутри» до
Ubuntu 22.04 — это смена дистрибутива, а не обновление.

---

## 4. Выбор площадки: три варианта

### Вариант A (рекомендуется). Новая KVM-ВМ на PROXMOX3

**Плюсы:** гипервизор уже есть; ресурсы есть (см. 5.2); собственное ядро ВМ
не зависит от старого ядра хоста; совместимость доказана работающей
ВМ 120 `ubuntu22`; старый сервер остаётся рядом для параллельной работы.

**Минусы:** PROXMOX3 сам устарел (PVE 4.4, 2016 г.) и является единой точкой
отказа — на нём же работает старый Zabbix. QEMU 2.7 не поддерживает
современные типы машин, ставить придётся с `machine=pc-i440fx-2.7`.

**Ограничение по ресурсам:** RAM хоста 157 ГБ, гостям выделено ≈ 82 ГБ,
кэш ZFS ARC занимает 78,7 ГБ. Свободно по `MemAvailable` — 17,2 ГБ.
ARC ужимается автоматически, но правильнее ограничить его явно
(см. 5.2), освободив запас под новую ВМ.

### Вариант B. LXC-контейнер с современной ОС — **невозможен**

На PROXMOX3 ядро 4.4 (2016 г.). Контейнер разделяет ядро с хостом, а
systemd версий 247+ (Ubuntu 22.04, Debian 12) требует унифицированной
иерархии cgroup v2, которой в ядре 4.4 нет. Такой контейнер не стартует.
Подтверждение: в хранилище шаблонов PROXMOX3 лежат только CentOS 6/7 и
Debian 8 — новее туда просто не ставили.

Вариант становится возможным только после обновления самого Proxmox, что
является отдельным проектом с остановкой всех 30+ гостей.

### Вариант C. Отдельный физический или облачный сервер

**Плюсы:** мониторинг перестаёт зависеть от наблюдаемой им же
инфраструктуры — правильная практика: сейчас Zabbix живёт на PROXMOX3 и
при падении этого хоста мы теряем и сервисы, и наблюдение за ними.

**Минусы:** требует закупки/выделения оборудования.

**Рекомендация:** выбрать вариант A сейчас, а вынос мониторинга на
независимую площадку заложить в план как отдельную задачу.

---

## 5. Технические требования к новой виртуальной машине

### 5.1 Конфигурация ВМ

| Параметр | Значение | Обоснование |
|---|---|---|
| VMID | `141` (свободен; занятые — до 140) | — |
| Имя | `zabbix8` | — |
| vCPU | **4** (`cores: 4`, `sockets: 1`) | текущая нагрузка ≈ 43 значения/сек при 2 561 элементе; 4 ядра дают трёхкратный запас под рост до 200+ хостов |
| Тип CPU | `kvm64` или `host` | QEMU 2.7; `host` быстрее, но мешает миграции между узлами — узел один, поэтому допустимо |
| RAM | **8192 МБ** | Zabbix server ~1 ГБ + MariaDB buffer pool 4 ГБ + PHP-FPM/nginx ~1 ГБ + запас |
| Диск | **100 ГБ**, `local-zfs`, формат raw, `discard=on` | БД сейчас 1,5 ГБ; при удвоении числа хостов и той же глубине хранения — ~15 ГБ; 100 ГБ покрывают 5+ лет |
| Сеть | `virtio`, мост `vmbr0` | сеть офиса 192.168.0.0/24 |
| IP | **192.168.0.111/24**, шлюз `192.168.0.101` | адрес освобождается: закреплён за неиспользуемым CT 130 `zabbixt` (остановлен, в описании «not used») |
| DNS | `192.168.0.103`, `192.168.0.102` | как у действующего контейнера |
| Тип машины | `pc-i440fx-2.7` | QEMU 2.7 не знает более новых |
| Загрузка | ISO `ubuntu-22.04.3-live-server-amd64.iso` | уже лежит в `/var/lib/vz/template/iso/` |
| Автозапуск | `onboot: 1` | мониторинг должен подниматься сам |

Команда создания (выполняется на PROXMOX3):

```bash
qm create 141 --name zabbix8 --memory 8192 --cores 4 --sockets 1 \
  --net0 virtio,bridge=vmbr0 --ostype l26 --machine pc-i440fx-2.7 \
  --scsihw virtio-scsi-pci --scsi0 local-zfs:100,discard=on \
  --ide2 local:iso/ubuntu-22.04.3-live-server-amd64.iso,media=cdrom \
  --boot order='ide2;scsi0' --onboot 1 --agent 1
qm start 141
```

### 5.2 Подготовка гипервизора

Кэш ZFS ARC занимает 78,7 ГБ при лимите 78,7 ГБ. Он отдаёт память под
запросы гостей, но при старте ВМ на 8 ГБ возможна кратковременная
деградация ввода-вывода. Рекомендуется заранее ограничить ARC 64 ГБ:

```bash
echo "options zfs zfs_arc_max=68719476736" >> /etc/modprobe.d/zfs.conf
update-initramfs -u          # применится после перезагрузки хоста
echo 68719476736 > /sys/module/zfs/parameters/zfs_arc_max   # сразу, без перезагрузки
```

**Отдельно и срочно, вне рамок миграции:** хранилище `storage` заполнено
на **99,22 %**, `ctx` — на 98,93 %. Это уже висит в Zabbix как Disaster.
Новую ВМ размещаем на `local-zfs` (свободно 5,34 ТБ), но переполненные
пулы нужно разгружать независимо от миграции.

### 5.3 Программный стек новой ВМ

| Компонент | Версия | Источник |
|---|---|---|
| ОС | Ubuntu 22.04.3 LTS (jammy) | ISO на гипервизоре |
| Zabbix server / frontend / agent2 | **8.0 LTS** | `repo.zabbix.com/zabbix/8.0/release/ubuntu`, пакет `zabbix-release_8.0-0.2+ubuntu22.04_all.deb` |
| СУБД | MariaDB 10.6 | репозиторий Ubuntu |
| Веб-сервер | nginx 1.18 + PHP-FPM 8.1 | репозиторий Ubuntu |
| Java gateway | `zabbix-java-gateway` | из репозитория Zabbix — **только если решено чинить 87 JMX-элементов** |
| Дополнительно | `fping`, `snmp`, `snmp-mibs-downloader`, `unixodbc`, `OpenIPMI`, `smartmontools`, `curl` | репозиторий Ubuntu |
| Для Oracle-скриптов | `python3-oracledb` (тонкий режим, Instant Client не требуется) | pip/apt |

Zabbix 8.0 — версия с долгосрочной поддержкой, следующая после 7.0 LTS.
Ubuntu 22.04 входит в её матрицу поддержки (проверено в репозитории
12.09.2026). Ubuntu 24.04 LTS даёт более длинный срок поддержки, но её ISO
на гипервизоре нет, а совместимость с QEMU 2.7 не проверена — при желании
использовать 24.04 требуется отдельная проверка загрузки тестовой ВМ.

### 5.4 Требования к сети и доступам

| Что | Куда | Зачем |
|---|---|---|
| Новая ВМ → `192.168.0.0/24:10050` | все агенты | опрос пассивных агентов |
| Агенты → новая ВМ `:10051` | 282 активных элемента | активные проверки |
| Новая ВМ → `api.telegram.org:443` | интернет | отправка уведомлений (проверено: с текущего контейнера работает) |
| Новая ВМ → `repo.zabbix.com`, `archive.ubuntu.com` | интернет | установка пакетов |
| Новая ВМ → `orange.una.md:4024`, `192.168.0.18:1521` | Oracle | скрипты мониторинга БД |
| Рабочие места → новая ВМ `:80` | LAN | веб-интерфейс |
| Старый `192.168.0.110` ↔ новый `192.168.0.111` | LAN | перенос дампа конфигурации |

---

## 6. Стратегия переноса данных

### 6.1 Что переносим обязательно

1. **Конфигурация** (21,3 МБ): хосты, шаблоны, элементы, триггеры, графики,
   действия, пользователи, группы, веб-сценарии, карты.
2. **Внешние скрипты** (`/usr/lib/zabbix/externalscripts`, 8 файлов).
3. **Alert-скрипт Telegram** вместе с токеном бота — иначе уведомления
   не заработают.
4. **Настройки сервера** (`zabbix_server.conf`): `StartPollers=20`,
   `StartPingers=5`, `StartDiscoverers=5`, `StartHTTPPollers=5`,
   `CacheSize=256M`, `Timeout=30`.

### 6.2 Способ переноса конфигурации — два пути

| | Путь 1: экспорт/импорт через API | Путь 2: поэтапный апгрейд БД |
|---|---|---|
| Суть | выгрузить шаблоны и хосты из 3.4, загрузить в 8.0 | дамп БД → MariaDB 10.6 → запуск Zabbix 6.0 (обновит схему) → 7.0 → 8.0 |
| История | **не переносится** | **переносится полностью** |
| Риск | низкий: новая БД чистая, ошибки видны сразу | высокий: 3 последовательных апгрейда схемы на 1,5 ГБ; несовместимость форматов 3.4 |
| Время | 3–5 часов | 6–10 часов, из них апгрейды схемы непредсказуемы |
| Побочный эффект | **автоматически отсеивается мусор** (72 мёртвых элемента) | мусор переносится как есть |
| Форматы | XML 3.4 → требует конвертации в YAML 8.0 для части объектов | — |

**Рекомендуется путь 1** — при 21 МБ конфигурации и 90-дневной глубине
истории риск поэтапного апгрейда не оправдан.

### 6.3 Что делать с историей

Рекомендуемый компромисс — **параллельная работа**:

1. Новый сервер запускается и начинает копить историю с нуля.
2. Старый сервер остаётся включённым в режиме наблюдения ещё 2 недели:
   графики за прошлый период смотрим на нём.
3. Через 2 недели старый сервер останавливается, но контейнер **не
   удаляется** ещё 90 дней — на случай, если понадобится заглянуть в историю.
4. Перед удалением снимается дамп БД в архив (1,5 ГБ).

Годовые тренды (`trends`, `trends_uint`) при этом теряются. Если история
за год критична для отчётности — выбирается путь 2 из 6.2, и это решение
нужно принять **до** начала работ.

---

## 7. План работ по этапам

### Этап 0. Подготовка (0,5 дня, без влияния на работу)

| # | Действие | Проверка результата |
|---|---|---|
| 0.1 | Снять дамп БД старого Zabbix | `mysqldump -uroot zabbix \| gzip > /root/zabbix34-$(date +%F).sql.gz`, размер ≈ 300–400 МБ |
| 0.2 | Заархивировать `/etc/zabbix` и `/usr/lib/zabbix` | tar-архив на внешнем носителе |
| 0.3 | Выгрузить конфигурацию через API | скрипт `netmon_zabbix_export.py` (см. раздел 11) |
| 0.4 | Зафиксировать токен Telegram-бота | из `telegram_bot.sh`, хранить вне репозитория |
| 0.5 | Ограничить ZFS ARC на гипервизоре | `cat /sys/module/zfs/parameters/zfs_arc_max` = 68719476736 |
| 0.6 | Согласовать решение по истории (6.3) | письменное решение владельца |

### Этап 1. Развёртывание ВМ (0,5 дня)

| # | Действие | Проверка |
|---|---|---|
| 1.1 | Создать ВМ 141 командой из 5.1 | `qm status 141` = running |
| 1.2 | Установить Ubuntu 22.04, статический IP `192.168.0.111` | `ping 192.168.0.111` |
| 1.3 | Обновить систему, поставить `qemu-guest-agent` | ВМ видна в Proxmox с IP |
| 1.4 | Установить MariaDB, создать БД `zabbix` с **сильным паролем** | текущий пароль `zabbix` — повторять нельзя |
| 1.5 | Подключить репозиторий Zabbix 8.0, поставить server + frontend + agent2 | `zabbix_server -V` = 8.0.x |
| 1.6 | Настроить nginx + PHP-FPM, открыть веб-интерфейс | вход в веб по `http://192.168.0.111` |

```bash
# 1.5 — подключение репозитория и установка
wget https://repo.zabbix.com/zabbix/8.0/release/ubuntu/pool/main/z/zabbix-release/zabbix-release_8.0-0.2+ubuntu22.04_all.deb
dpkg -i zabbix-release_8.0-0.2+ubuntu22.04_all.deb && apt update
apt install -y zabbix-server-mysql zabbix-frontend-php zabbix-nginx-conf \
               zabbix-sql-scripts zabbix-agent2 fping snmp unixodbc
```

### Этап 2. Перенос конфигурации (1 день)

| # | Действие | Проверка |
|---|---|---|
| 2.1 | Создать группы хостов и пользователей | 18 групп, 6 пользователей |
| 2.2 | Импортировать собственные шаблоны (7 шт.) | шаблоны видны в списке |
| 2.3 | Импортировать хосты пакетами по группам | 77 хостов, ошибок импорта 0 |
| 2.4 | Перенести внешние скрипты, выставить права | `ls -l /usr/lib/zabbix/externalscripts` |
| 2.5 | **Портировать `pyora.py` на Python 3 + `oracledb`** | запуск вручную возвращает значение |
| 2.6 | Перенести `telegram_bot.sh` с токеном | тестовая отправка в канал приходит |
| 2.7 | Создать media type и действие «Telegram» | условие severity ≥ average, как сейчас |
| 2.8 | Перенести правило сетевого обнаружения | — |
| 2.9 | Применить параметры `zabbix_server.conf` из 6.1 | сервер стартует без ошибок в логе |

### Этап 3. Параллельная работа и проверка (2 недели)

| # | Действие | Критерий приёмки |
|---|---|---|
| 3.1 | Переключить агенты: добавить новый сервер в `Server=` и `ServerActive=` | агенты отвечают обоим серверам |
| 3.2 | Сверить число собираемых значений на двух серверах | расхождение < 5 % |
| 3.3 | Учебное отключение тестового узла | уведомление приходит в Telegram **с нового** сервера |
| 3.4 | Проверить каждый тип элементов из 2.4 | неподдерживаемых элементов — не больше, чем на старом |
| 3.5 | Прогнать приёмочные испытания модуля netmon против нового сервера | `netmon_selftest.py` — 0 отказов |
| 3.6 | Перевести модуль `netmon` на новый адрес API | панель показывает данные нового сервера |

### Этап 4. Переключение и вывод старого (0,5 дня)

| # | Действие | Проверка |
|---|---|---|
| 4.1 | Отключить действие «Telegram» на **старом** сервере | дубли уведомлений прекратились |
| 4.2 | Убрать старый сервер из конфигурации агентов | — |
| 4.3 | Остановить `zabbix-server` на CT 101, оставить контейнер | мониторинг работает только на новом |
| 4.4 | Наблюдать 90 дней, затем архивировать и удалить CT 101 | дамп в архиве |

---

## 8. Тесты приёмки

Миграция считается завершённой, когда выполнено **всё**:

1. На новом сервере **77 хостов** и не меньше 2 489 работающих элементов
   (2 561 включённых минус 72 заведомо сломанных).
2. Уведомление о реальной проблеме пришло в канал
   «Zabbix Alert: Unisim-Soft&Coninfo» именно с нового сервера.
3. Веб-интерфейс открывается, все 6 учётных записей заходят.
4. Внешние скрипты возвращают значения (особенно портированный `pyora.py`).
5. Приёмочный прогон `netmon_selftest.py` — 0 отказов.
6. Автозапуск: после перезагрузки ВМ сервер и БД поднимаются сами.
7. Резервное копирование БД настроено (см. раздел 10).

---

## 9. План отката

Откат прост, потому что старый сервер всё время остаётся рабочим:

| Ситуация | Действие | Время |
|---|---|---|
| Новый сервер не собирает данные | продолжаем на старом, разбираемся без спешки | 0 мин |
| Уведомления не уходят с нового | включить обратно действие на старом сервере | 5 мин |
| Новая ВМ не стартует после обновления | восстановить из снимка Proxmox | 15 мин |
| Нужно всё вернуть | запустить `zabbix-server` на CT 101, вернуть `Server=` агентов | 30 мин |

**Обязательное условие:** перед этапом 4 снять снимок ВМ 141
(`qm snapshot 141 before-cutover`) и не удалять CT 101 раньше 90 дней.

---

## 10. Что улучшить заодно

Миграция — единственный удобный момент исправить то, что сейчас неправильно:

1. **Пароль БД.** Сейчас `DBPassword=zabbix` — словарный. На новом сервере
   сгенерировать случайный, хранить в Keychain.
2. **Резервное копирование.** Сейчас регулярного бэкапа БД Zabbix нет.
   Настроить ежедневный дамп конфигурации (без истории — это 21 МБ) с
   хранением 30 копий.
3. **Мусор в конфигурации.** Не переносить 38 мёртвых ODBC-элементов и
   87 неопрашиваемых JMX; решить по каждому: чиним или списываем.
4. **Java gateway.** Либо поставить и вернуть JMX-мониторинг к жизни, либо
   осознанно отказаться и удалить элементы.
5. **Шифрование агентов.** Zabbix 8.0 поддерживает PSK/сертификаты для
   связи с агентами; сейчас трафик открыт.
6. **Независимость мониторинга.** Отдельной задачей вынести Zabbix с
   PROXMOX3: сейчас падение этого хоста лишает и сервисов, и наблюдения.
7. **Переполненные хранилища** `storage` (99,22 %) и `ctx` (98,93 %) —
   разгрузить независимо от миграции.

---

## 11. Инструменты, подготовленные для миграции

| Инструмент | Назначение |
|---|---|
| `modules/netmon/scripts/netmon_zabbix_export.py` | выгрузка конфигурации действующего Zabbix: хосты, шаблоны, действия, пользователи, скрипты — в файлы для переноса и как резервная копия |
| `modules/netmon/scripts/netmon_selftest.py` | приёмочные испытания; после миграции запускается против нового сервера |
| `modules/netmon/sources.py` | клиент Zabbix API; при переключении меняется единственная константа `ZBX_URL` |

---

## 12. Итоговая смета времени

| Этап | Трудозатраты | Простой мониторинга |
|---|---|---|
| 0. Подготовка | 4 часа | нет |
| 1. Развёртывание ВМ | 4 часа | нет |
| 2. Перенос конфигурации | 8 часов | нет |
| 3. Параллельная работа | 2 недели наблюдения, ~4 часа проверок | нет |
| 4. Переключение | 2 часа | нет |
| **Итого** | **≈ 22 часа работ** | **0 минут** |

Отдельно: портирование `pyora.py` на Python 3 — от 2 до 6 часов в
зависимости от объёма скрипта.

---

## 13. Решения, которые нужны от владельца

1. **История за год.** Терять годовые тренды (путь 1, быстрый и
   безопасный) или переносить историю поэтапным апгрейдом БД
   (путь 2, дольше и рискованнее)? — определяет раздел 6.
2. **JMX-мониторинг.** 87 элементов не работают уже сейчас. Чиним на новом
   сервере (нужен Java gateway) или списываем?
3. **Oracle-мониторинг.** 38 ODBC-элементов сломаны полностью. Восстанавливаем
   или отказываемся?
4. **Площадка.** Согласен ли вариант A (та же PROXMOX3) или планируем сразу
   выносить мониторинг на независимый сервер (вариант C)?
5. **Окно работ.** Простоя мониторинга не требуется, но этап 4 лучше
   проводить в рабочее время, когда есть кому наблюдать.
