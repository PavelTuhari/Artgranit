# Инженерное оборудование офиса: учёт, обслуживание, фотофиксация

Заведено 12.09.2026. Раздел панели «Оборудование офиса»:
https://nufarul.eminescu.md/UNA.md/orasldev/netmon (локально —
http://127.0.0.1:3013/UNA.md/orasldev/netmon).

---

## 1. Зачем это отдельно от мониторинга сети

Zabbix отвечает на вопрос «жива ли железка в сети». Но кондиционер, котёл
и лифт в сеть не включены: их состояние знает только человек, который к ним
подошёл. При этом отказ кондиционера в серверной опаснее, чем падение
половины рабочих станций.

Поэтому здесь другой принцип: **паспорт объекта + журнал работ +
фотографии от сотрудников**, а не опрос по сети.

---

## 2. Что стоит на учёте — 11 объектов

| Код | Тип | Помещение | Регламент | Примечание |
|---|---|---|---|---|
| `AC-01` | Кондиционер | Серверная | 90 дней | **критичный**: держит температуру стойки с cloudbd и PROXMOX3 |
| `AC-02` | Кондиционер | Кабинет разработки | 90 дней | |
| `AC-03` | Кондиционер | Бухгалтерия | 90 дней | |
| `AC-04` | Кондиционер | Переговорная | 90 дней | |
| `BOILER-01` | Котёл отопления | Техническое помещение | 365 дней | проверка перед сезоном; в сеть не подключён |
| `ACS-01` | Контроль доступа ZKAccess | Вход | 180 дней | **виртуальная машина 133 на PROXMOX3 остановлена**, контроллер в сети не отвечает |
| `LIFT-01` | Кнопка управления лифтом | Холл | 30 дней | осмотр ежемесячно, обслуживает сторонняя организация |
| `PLUG-203` … `PLUG-240` | Умные розетки (4 шт.) | уточнить | 365 дней | в сети, управление — см. [SMART_PLUGS.md](SMART_PLUGS.md) |

### Что требует решения

1. **Система контроля доступа не работает как сервис.** ВМ 133 «ZKAcces»
   остановлена, порт 4370 в сети не отвечает. Либо контроллер работает
   автономно (тогда журнал проходов не собирается), либо СКУД отключена.
2. **Помещения розеток не заполнены** — надо пройти и посмотреть, что в них
   включено.
3. Модели, серийные номера и даты установки не заполнены ни у одного
   объекта: их вносят через паспорт по мере обслуживания.

---

## 3. Регламент обслуживания

Сроки заложены в модуле (`modules/netmon/facility.py`, `SERVICE_RULES`):

| Тип | Вид работ | Периодичность |
|---|---|---|
| Кондиционер | чистка фильтров | **90 дней** |
| Кондиционер | заправка фреоном | **730 дней** (2 года) |
| Кондиционер | осмотр | 180 дней |
| Котёл | осмотр | 365 дней (перед сезоном) |
| Контроль доступа | осмотр | 180 дней |
| Лифт | осмотр | **30 дней** |
| Умная розетка | осмотр | 365 дней |

Панель сама считает, когда срок вышел: «просрочено на N дней» красным,
«через N дней» жёлтым при приближении, «работ не зафиксировано» — если по
объекту вообще нет записей.

---

## 4. Как вести журнал

### Через панель (обычный путь)

1. Вкладка «Оборудование офиса» → клик по строке → откроется паспорт.
2. Внизу «Добавить запись»: выбрать вид работ, описать что сделано, указать
   исполнителя → **Записать**.
3. Дата следующего обслуживания проставляется **автоматически** по
   регламенту: чистка фильтров — плюс 90 дней, фреон — плюс 2 года.

### Фотография состояния

В том же паспорте: поле выбора файла открывает **камеру телефона**
(`capture="environment"`), подпись — что на снимке. Снимок привязывается к
объекту и виден в его карточке с указанием, кто и когда снял.

Ограничения: форматы jpg, jpeg, png, heic, webp; не больше 12 МБ. Файлы
лежат на диске сервера (`modules/netmon/photos/`), в базе — только путь:
снимки занимают мегабайты, а Oracle нужен для связей, не для картинок.

### Через API

| Метод | Адрес | Что делает |
|---|---|---|
| `GET` | `…/netmon/api/facilities` | реестр со сроками обслуживания |
| `GET` | `…/netmon/api/facilities/<код>` | паспорт: журнал и фотографии |
| `POST` | `…/netmon/api/facilities/<код>/work` | добавить запись о работах |
| `POST` | `…/netmon/api/facilities/<код>/photo` | загрузить снимок |
| `GET` | `…/netmon/photo/<id>` | получить снимок |

Пример записи о чистке фильтров:

```bash
curl -b cookies.txt -X POST \
  https://nufarul.eminescu.md/UNA.md/orasldev/netmon/api/facilities/AC-01/work \
  -H 'Content-Type: application/json' \
  -d '{"work_kind":"filter","description":"Промыты фильтры, проверен дренаж","performer":"Иванов"}'
```

Виды работ: `filter` (чистка фильтров), `freon` (заправка фреоном),
`inspect` (осмотр), `repair` (ремонт), `install` (установка), `other`.

Загрузка снимка:

```bash
curl -b cookies.txt -X POST \
  https://nufarul.eminescu.md/UNA.md/orasldev/netmon/api/facilities/AC-01/photo \
  -F "photo=@filter.jpg" -F "caption=Фильтр после чистки"
```

---

## 5. Модель данных (Oracle, префикс `NMON_`)

| Таблица | Назначение |
|---|---|
| `NMON_FACILITIES` | паспорт объекта: код, тип, помещение, модель, серийный номер, адрес в сети, регламент, ответственный |
| `NMON_FAC_LOG` | журнал работ: вид, дата, исполнитель, описание, дата следующего раза, стоимость |
| `NMON_FAC_PHOTO` | снимки: путь к файлу, подпись, кто снял, когда, размер |

Текстовые колонки объявлены в **символьной семантике** (`VARCHAR2(N CHAR)`):
по умолчанию Oracle считает байты, и русский текст в короткие поля не влезает.

Установка: `python modules/netmon/scripts/netmon_deploy.py`.
Первичное заполнение: `python modules/netmon/scripts/netmon_facility_seed.py`
(повторный запуск безопасен — обновляет паспорта, журнал не трогает).

---

## 6. Проверено вживую 12.09.2026

1. Заведено 11 объектов, распределение по типам совпадает с обследованием.
2. Записаны две работы по `AC-01`: чистка фильтров (повтор назначен на
   11.12.2026) и заправка фреоном (повтор 11.09.2028) — сроки посчитаны
   автоматически.
3. Загружен снимок, открывается в карточке объекта.
4. Ловушка, на которую напоролись: **`ORA-01745` на имени bind-переменной
   `:by`** — `BY` зарезервировано в Oracle. Переименовано в `:usr`. Та же
   ошибка уже встречалась в TBControl и записана в `CLAUDE.md`.

---

## 7. Оборудование в Zabbix (12.09.2026)

Всё новое оборудование заведено в Zabbix, поэтому его состояние попадает в
Telegram тем же путём, что и серверы. Группа — **Office Facility**.

### 7.1 Умные розетки: опрашиваются напрямую

| Хост | Адрес | Что проверяется |
|---|---|---|
| `plug-203` | 192.168.0.203 | ICMP + порт 6668 |
| `plug-206` | 192.168.0.206 | ICMP + порт 6668 |
| `plug-207` | 192.168.0.207 | ICMP + порт 6668 |
| `plug-240` | 192.168.0.240 | ICMP + порт 6668 |

Розетки уже были в Zabbix после первого скана, но под именами `net-203…240`
и с классом «сетевое оборудование» — по TTL их не отличить от коммутатора.
Скрипт **переименовал существующие хосты, а не создал дубли**.

Кроме пинга проверяется **порт 6668** (`net.tcp.service`): он отвечает на
вопрос, жива ли служба управления, а не только сама железка. Триггер
«служба управления не отвечает» (average) срабатывает, когда розетка
пингуется, но командами не управляется — питание есть, управления нет.

Классификатор `rules.py` теперь узнаёт такие устройства: порт 6668/6667/8899
(Tuya) или 9999 (TP-Link Kasa) проверяется **до** правила по TTL, иначе
розетка снова уехала бы в «сетевое оборудование».

### 7.2 Оборудование вне сети: хост `facility-metrics`

Кондиционеры, котёл, СКУД и лифт в сеть не включены, поэтому опрашивать
нечего. Вместо этого модуль считает, сколько дней осталось до регламентного
обслуживания, и присылает число в Zabbix (`zabbix_sender`).

| Элемент | Значение |
|---|---|
| `facility.days[AC-01]` … `[AC-04]` | дней до обслуживания кондиционеров |
| `facility.days[BOILER-01]` | котёл |
| `facility.days[ACS-01]` | контроль доступа |
| `facility.days[LIFT-01]` | лифт |

Отрицательное число — просрочка. Триггеры: **просрочено** (average) и
**подходит срок, меньше 7 дней** (warning).

Если какой-то вид работ не выполнялся ни разу, берётся `−интервал`, то есть
«срок вышел ровно один период назад». Это честнее, чем «минус бесконечность»:
одна невыполненная работа не затирает реальные сроки по остальным.

### 7.3 Ловушка, которую поймали при настройке

Элементы сначала завели с типом **unsigned integer** — и Zabbix превратил
все отрицательные значения в **0**: просрочка не показалась бы никогда.
Тип исправлен на **numeric float** у всех 12 элементов (включая домены в
`business-metrics`, где та же ошибка выстрелила бы в день истечения домена).

### 7.4 Что сейчас показывает Zabbix

Первая отправка дала **7 сработавших триггеров** — по всем объектам, кроме
розеток:

| Объект | Просрочка | Почему |
|---|---|---|
| Кондиционеры кабинетов (3 шт.) | −730 дней | заправка фреоном не фиксировалась |
| Кондиционер серверной | −180 дней | осмотр не фиксировался (фильтры и фреон внесены 12.09) |
| Котёл | −365 дней | осмотр не фиксировался |
| Контроль доступа | −180 дней | осмотр не фиксировался |
| Лифт | −30 дней | осмотр не фиксировался |

**Это не поломка оборудования, а пустой журнал.** Работы наверняка велись —
просто нигде не записывались. Как только по объекту внесут первую запись,
срок пересчитается от неё, и триггер погаснет.

Команда обновления (после внесения работ):

```bash
cd /Users/pt/Projects.AI/Artgranit-netmon
venv/bin/python modules/netmon/scripts/netmon_zabbix_facility.py
```
