# 🔧 Исправление доступа по локальному IP

## Проблема

Приложение слушает только на `127.0.0.1:3003` (localhost), а не на `0.0.0.0:3003` (все интерфейсы).

## Решение

### 1. Остановите текущий процесс приложения

Найдите и остановите процесс, который использует порт 3003:

```bash
# Найти процесс
lsof -ti:3003

# Остановить процесс
lsof -ti:3003 | xargs kill -9
```

Или просто нажмите `Ctrl+C` в терминале, где запущено приложение.

### 2. Проверьте конфигурацию

Убедитесь, что в `config.py` установлено:

```python
SERVER_HOST = os.environ.get('SERVER_HOST', '0.0.0.0')
```

### 3. Перезапустите приложение

```bash
./run_local.sh
```

### 4. Проверьте, что приложение слушает на всех интерфейсах

После запуска выполните:

```bash
netstat -an | grep 3003 | grep LISTEN
```

Вы должны увидеть:
```
tcp4       0      0  *.3003                 *.*                    LISTEN
```

или

```
tcp4       0      0  0.0.0.0.3003           *.*                    LISTEN
```

**НЕ** должно быть:
```
tcp4       0      0  127.0.0.1.3003         *.*                    LISTEN
```

### 5. Проверьте доступность

Откройте в браузере на другом устройстве в той же сети:
```
http://192.168.100.132:3003/UNA.md/orasldev/dashboard
```

### Диагностика

Если после перезапуска все еще не работает, запустите скрипт диагностики:

```bash
./test_network_access.sh
```

## Альтернативное решение

Если по какой-то причине не работает доступ по IP, можно использовать проброс портов через SSH:

```bash
# На Mac, где запущено приложение
ssh -R 3003:localhost:3003 user@other-device
```

Но это более сложное решение. Проще всего - перезапустить приложение с правильными настройками.

