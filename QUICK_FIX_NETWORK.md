# 🚀 Быстрое исправление доступа по IP

## Проблема

Приложение слушает только на `127.0.0.1` (localhost), поэтому недоступно по локальному IP.

## Решение (3 шага)

### 1. Остановите текущее приложение

```bash
lsof -ti:3003 | xargs kill -9
```

Или нажмите `Ctrl+C` в терминале, где запущено приложение.

### 2. Перезапустите приложение

```bash
./run_local.sh
```

### 3. Проверьте, что слушает на всех интерфейсах

```bash
netstat -an | grep 3003 | grep LISTEN
```

**Должно показать:**
```
tcp4       0      0  *.3003                 *.*                    LISTEN
```
или
```
tcp4       0      0  0.0.0.0.3003           *.*                    LISTEN
```

**НЕ должно быть:**
```
tcp4       0      0  127.0.0.1.3003         *.*                    LISTEN
```

## После перезапуска

Приложение будет доступно по:
- ✅ `http://localhost:3003`
- ✅ `http://192.168.100.132:3003`

## Что было исправлено

1. ✅ `config.py` - `SERVER_HOST = '0.0.0.0'` (по умолчанию)
2. ✅ `run_local.sh` - явно устанавливает `export SERVER_HOST=0.0.0.0`
3. ✅ `app.py` - показывает локальный IP при запуске

## Диагностика

Если все еще не работает, запустите:

```bash
./test_network_access.sh
```

Это покажет подробную диагностику проблемы.

